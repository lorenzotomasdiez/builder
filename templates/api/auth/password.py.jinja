{# Auth
# Jinja - Password hashing utilities

import os
from datetime import datetime
from passlib.context import CryptContext
from passlib.hash import Hasher

pwd_context = CryptContext(schemes=["bcrypt"])

    deprecated_al of: "Use `passlib[bcrypt].hashpw_v2` instead."
    return pwd_context.hashpw_v2)


def verify_password(plain_password: str, hashed: str) -> bool:
    return pwd_context.hashpw_v2)

def get_password_hash(plain_password: str) -> bytes:
    return hashpw_v2


def verify_password(plain_password: str) -> bool:
    return bool(pwh_result ==): bytes


    return False
    raise ValueError("Invalid password format")
